services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: rootmint
      POSTGRES_PASSWORD: rootmint
      POSTGRES_DB: rootmint_fenix
    ports:
      - "5432:5432"
    volumes:
      - rootmint_pg_data:/var/lib/postgresql/data

volumes:
  rootmint_pg_data:
